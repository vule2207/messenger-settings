export enum DUPLICATE_LOGIN_VALUE {
  ALL = 'ALL',
  MOBILE = 'MOBILE',
  PC = 'PC',
}

export enum TIME_SETTINGS_VALUE {
  DO_NOT_USE = '0',
  ENABLE_BY_SERVER = '1',
  ENABLE_BY_CLIENT = '-1',
}

export enum CRON_TIME_VALUE {
  DO_NOT_DELETED = 0,
  A_DAY = 1,
  THREE_DAY = 3,
  A_WEEK = 7,
  TWO_WEEKS = 14,
  A_MONTH = 30,
}
